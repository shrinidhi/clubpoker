plugins {
    // If you are changing the Android Gradle Plugin version, make sure it is compatible with the Gradle version preinstalled with Unity
    // See which Gradle version is preinstalled with Unity here https://docs.unity3d.com/Manual/android-gradle-overview.html
    // See official Gradle and Android Gradle Plugin compatibility table here https://developer.android.com/studio/releases/gradle-plugin#updating-gradle
    // To specify a custom Gradle version in Unity, go do "Preferences > External Tools", uncheck "Gradle Installed with Unity (recommended)" and specify a path to a custom Gradle version
    id 'com.android.application' version '7.4.2' apply false
    id 'com.android.library' version '7.4.2' apply false
    **BUILD_SCRIPT_DEPS**
}

// Unity IAP (com.android.billingclient:billing) pulls kotlin-stdlib 1.8.22, which merged the
// jdk7/jdk8 artifacts into the main one. Older transitive kotlin-stdlib-jdk7/jdk8 1.6.21 still
// carry those classes, causing :launcher:checkReleaseDuplicateClasses to fail.
// Forcing every kotlin-stdlib artifact to 1.8.22 makes jdk7/jdk8 resolve to empty compat stubs.
allprojects {
    configurations.all {
        resolutionStrategy {
            force 'org.jetbrains.kotlin:kotlin-stdlib:1.8.22'
            force 'org.jetbrains.kotlin:kotlin-stdlib-jdk7:1.8.22'
            force 'org.jetbrains.kotlin:kotlin-stdlib-jdk8:1.8.22'
            force 'org.jetbrains.kotlin:kotlin-stdlib-common:1.8.22'

            // billing:9.0.0 pulls androidx.core 1.15.0, which the D8 shipped with
            // AGP 7.4.2 (Unity 2022.3) fails to dex. 1.13.1 is the last version that dexes.
            force 'androidx.core:core:1.13.1'
            force 'androidx.core:core-ktx:1.13.1'
        }
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
